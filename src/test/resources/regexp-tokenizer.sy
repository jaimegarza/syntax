%token A /_?[A-Za-z][A-Za-z0-9-]*/
%token B = if ($c == 'A') {$+; return B;}
%token C = if ($c == 'C') {$+; return C;}
%token D = if ($c == 'D') {$+; return D;}

%lexer {
  // second portion
}
%start S;

%name S : "anything";
%%

S    : A
     | B
     | C
     | D
     ;

%%
